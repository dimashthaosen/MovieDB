.env
# Local-only SQLite snapshot. The deployed app serves live from TMDb, so this
# is no longer shipped — it's just an optional cache for offline tooling.
movies.db
__pycache__/
*.pyc
.venv/
venv/
.claude/settings.local.json
